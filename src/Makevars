# CXX_STD = CXX11
PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS) -DBAYESLOGIT_NDEBUG -DBAYESLOGIT_NTHROW
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

# Don't need DUSE_R for bayeslogit because the random variables are already using Rcpp which uses R seed
# -DUSE_R
